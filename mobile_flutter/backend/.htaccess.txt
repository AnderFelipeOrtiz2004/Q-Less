<IfModule mod_rewrite.c>
    RewriteEngine On
    # Si tus archivos están en la raíz del servidor, puedes comentar esta línea
    # RewriteBase /q-less/ 

    # 1. Manejar peticiones OPTIONS (Preflight)
    RewriteCond %{REQUEST_METHOD} OPTIONS
    RewriteRule ^(.*)$ $1 [R=200,L]
</IfModule>

# CORS: usar solo cors.php en cada endpoint (no Header set aquí; evita duplicados)