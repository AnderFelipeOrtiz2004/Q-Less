This folder should contain sound assets used by the app:
- click.mp3
- success.mp3

To automatically download example sounds, run from the repository root:

```bash
dart run tools/download_sounds.dart
```

After the files are present, run:

```bash
flutter pub get
flutter run
```
